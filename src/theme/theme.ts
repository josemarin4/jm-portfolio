import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#434371",
    },
    secondary: {
      main: "#70EE9C",
    },
    background: {
      default: "#79AEA3",
      paper: "#484041",
    },
    text: {
      primary: "#FFFFFF",
      secondary: "#B5F44A",
    },
  },
});

export default theme;
